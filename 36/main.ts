function make_tshirt(
    size : string,
    message :  string) {
        console.log(`Making a ${size} t-shirt with message "${message}" printed on it.`);
    } make_tshirt("meduim size", "code is life");
